//* funkcje do pracy z aplikacją

const model = require("./model") 

let animalsArray = model.animalsArray
let animalID = 0

module.exports = {
    add: (animal, color) => {
       
    },
    delete: (selectedID) => {
        
    },
    update: (selectedID) => {
        
    },
    getall: () => {
        return animalsArray
    }
}